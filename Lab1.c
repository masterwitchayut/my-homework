#include <stdio.h>

int main()
{
    int n, i;

    if (scanf("%d", &n) != 1)
    {
        return 1;
    }
    if (n <= 0)
    {
        printf("Error");
        return 1;
    }

    for (i = 0; i < n; i++)
    {
        A
            printf("Hello Loop!\n");
    }
    return 0;
}
